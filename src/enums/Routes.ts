export enum Routes {
  LOGIN = '/auth/login',
  RECOVER_ACCOUNT = '/auth/recover-account',
  CLIENTS = '/clients',
  USERS = '/users',
  LOCATIONS = '/locations',
  CHECKLISTS = '/checklists',
  CONFIG = '/config',
}
