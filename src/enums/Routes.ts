export enum Routes {
  LOGIN = '/auth/login',
  RECOVER_ACCOUNT = '/auth/recover-account',
  UPDATE_PASSWORD = '/update-password',
  CLIENTS = '/clients',
  USERS = '/users',
  LOCATIONS = '/locations',
  CHECKLISTS = '/checklists',
  CONFIG = '/config',
}
