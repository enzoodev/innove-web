export enum Routes {
  LOGIN = '/auth/login',
  RECOVER_ACCOUNT = '/auth/recover-account',
  CLIENTS = '/clients',
  USERS = '/users',
  LOCATIONS = '/locations',
  CHECKLISTS = '/checklists',
  CREATE_CHECKLIST = '/checklists/create',
  UPDATE_CHECKLIST = '/checklists/update',
  CONFIG = '/config',
  UPDATE_PASSWORD = '/config/update-password',
}
